<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lsl.wanandroid">
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.webView.WebActivity"></activity>
        <!-- 引导页 -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/FixSystemWindowTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 主页 -->
        <activity android:name=".ui.main.activity.MainActivity" /> <!-- 搜索 -->
        <activity android:name=".ui.search.activity.SearchActivity" /> <!-- 登录 -->
        <activity android:name=".ui.login.activity.LoginActivity" />
    </application>

</manifest>